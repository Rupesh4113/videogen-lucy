[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "videogen-lucy"
version = "1.0.0"
description = "AI Long-Form Video Generation Platform"
readme = "README.md"
requires-python = ">=3.10"

[tool.vercel]
entrypoint = "main:app"
