version: '3.8'

services:
  # Main App Server (CPU Backend + Web UI + Auth)
  videogen-app:
    build:
      context: ..
      dockerfile: docker/Dockerfile.backend
    container_name: videogen-production-app
    restart: always
    ports:
      - "8000:8000"
    environment:
      - DEBUG=false
      - SECRET_KEY=${SECRET_KEY:-videogen_super_secret_jwt_key_prod_2026}
      - ACCESS_TOKEN_EXPIRE_MINUTES=43200  # 30 days
      - DATABASE_URL=sqlite+aiosqlite:///storage/videogen.db
      - SMS_PROVIDER=${SMS_PROVIDER:-simulation}  # "simulation" or "twilio"
      - TWILIO_ACCOUNT_SID=${TWILIO_ACCOUNT_SID:-}
      - TWILIO_AUTH_TOKEN=${TWILIO_AUTH_TOKEN:-}
      - TWILIO_PHONE_NUMBER=${TWILIO_PHONE_NUMBER:-}
      - VIDEO_PROVIDER=simulation  # Set to "wan_local" or "replicate"
      - VOICE_PROVIDER=edge_tts
      - STORAGE_PROVIDER=local
      - DEFAULT_RESOLUTION=1080p
      - DEFAULT_FPS=24
    volumes:
      - videogen_storage:/app/storage
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/health"]
      interval: 15s
      timeout: 5s
      retries: 3
    networks:
      - videogen-net

  # NGINX Reverse Proxy with SSL (Optional standalone proxy)
  nginx:
    image: nginx:alpine
    container_name: videogen-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - certbot_certs:/etc/letsencrypt
    depends_on:
      - videogen-app
    networks:
      - videogen-net

volumes:
  videogen_storage:
  certbot_certs:

networks:
  videogen-net:
    driver: bridge
